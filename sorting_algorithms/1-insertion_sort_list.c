#include "sort.h"

/**
 * bubble_sort -  function that sorts a doubly linked list.
 * @array: Array
 * @size: size array
 *
 * Return: Always 0
 */

void insertion_sort_list(listint_t **list)
{
	print_list(*list);
}
